function operationOnX(){
    let x =2;
    console.log(`x declared as ${x}`);
    x+=2
    console.log(`after addition of 2, x becomes ${x}`);
    
    x-=6
    console.log(`after substraction of 6, x becomes ${x}`);
    
    x/=3
    console.log(`after divsion with 3, x becomes ${x}`);
    
    x*=9
    console.log(`after multiplication with 9, x becomes ${x}`);
    
}
operationOnX()